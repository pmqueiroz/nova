pre-commit:
  parallel: false
  jobs:
    - run: cargo clippy --fix --allow-dirty --allow-staged
      glob: "*.rs"
      stage_fixed: true
    - run: cargo fmt -- {staged_files}
      glob: "*.rs"
      stage_fixed: true
